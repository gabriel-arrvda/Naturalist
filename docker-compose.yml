version: '3.8'

services:
  api:
    build: .
    image: ghcr.io/${GITHUB_OWNER:-gabriel-arrvda}/naturalist:latest
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./:/app:ro
